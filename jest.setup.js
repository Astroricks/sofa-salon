require('@testing-library/jest-dom');

// To mock env vars in a test (e.g. for config or email):
// beforeEach(() => { process.env.NEXT_PUBLIC_APP_NAME = 'TestApp'; });
// afterEach(() => { delete process.env.NEXT_PUBLIC_APP_NAME; });
